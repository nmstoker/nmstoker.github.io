# test_fn is the function that will be run to test the student's proposed solution
test_fn: greater_num

# problem_description is displayed on the problem page and provides the doctest details used to test the student's
# proposed solution
problem_description: |
  Return whichever number is highest of the two supplied numbers.

  >>> greater_num(45, 10)
  45
  >>> greater_num(-1, 30)
  30
  >>> greater_num(20, 20)
  20

# code_lines are used to generate the code blocks that the student may include in their proposed solution
code_lines: |
  def greater_num(num1, num2): #0given
  if !BLANK:
  return !BLANK
  else:
  return !BLANK